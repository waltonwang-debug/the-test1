# PR Submission Note

This repository uses an integration-based PR creation flow in this environment.

- Changes are committed locally on the working branch.
- PR metadata is submitted through the GitHub integration tool.
- Direct `git push` is intentionally skipped per task instruction.
